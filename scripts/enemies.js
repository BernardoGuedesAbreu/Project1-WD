//class enemy test

class Enemy {
    constructor(x, y, appearance) {
      this.x = x;
      this.y = y;
      this.appearance = appearance; // we can randomize a number from like 1-12 and 
    }   //have the images be called 1 to 12
    // so we can have random looking enemies! 
    
    move() {
      // code to move the enemy
    }
    
    attack() {
      // code to make the enemy attack
    }
    
    // other enemy methods
  }
  
 


  console.log("enemies JS is loaded")